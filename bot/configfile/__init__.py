"""Módulo para inicialização do `configparse`
- Arquivo(s) `.ini` deve(m) estar presente(s) na raiz do projeto
- Usar variável com a sintaxe `${secao:opcao}`
- `$` é reservado, utilizar `$$` para contornar
- `#` comenta a linha se estiver no começo"""

from .__setup import *